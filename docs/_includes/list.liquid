{% assign command_pages = site.pages | where_exp: "item", "item.path contains 'commands'" | sort: "title" %}
{% for command in command_pages %}
  {% if command.title and command.title != "Commands" %}
- [{{ command.title }}]({{ command.url | relative_url }})
  {% endif %}
{% endfor %}
